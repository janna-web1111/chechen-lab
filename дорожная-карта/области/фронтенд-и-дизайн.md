# Roadmap: frontend и дизайн

Здесь фиксируются завершенные изменения во frontend:

- экраны;
- маршруты;
- компоненты;
- пользовательские потоки;
- состояние приложения;
- ошибки и пустые состояния.

Здесь же фиксируются завершенные изменения в интерфейсе:

- визуальные правила;
- layout;
- адаптивность;
- UI-состояния;
- UX-решения;
- изменения по удобству первого пути.

Новые записи добавляются сверху.

## 2026-06-10 - MVP-candidate check

- План: `../../планы-разработки/completed/2026-06-10-05-mvp-candidate-check.md`
- Проверен основной пользовательский путь после native review контента.
- Пользователь вручную подтвердил, что desktop/mobile layout работает правильно.
- Экран темы теперь показывает локализованный статус `Проверено` вместо raw-значения `reviewed`.
- Проверки:
  - `curl -I http://localhost:3000` вернул `200 OK` вне sandbox.
  - `npm run lint` прошел.
  - `npm run build` прошел вне sandbox.

## 2026-06-10 - UI-статус проверенного контента

- План: `../../планы-разработки/completed/2026-06-10-04-native-review-mvp-контента.md`
- Карточки и экран повторения теперь показывают бейдж `Проверено` для статуса `reviewed`.
- Красный статус `Ожидает проверки` сохранен только для будущего контента со статусом `needs_native_review`.
- Экран темы показывает, что чеченский контент проверен ответственным проверяющим.
- Проверки:
  - `npm run lint` прошел.
  - `npm run build` прошел вне sandbox.
  - `curl -I http://localhost:3000` вернул `200 OK` вне sandbox.

## 2026-06-08 - Первый MVP-поток и UX-проверка

- Планы:
  - `../планы-разработки/completed/2026-06-08-01-первый-тестируемый-поток-mvp.md`
  - `../планы-разработки/completed/2026-06-08-02-ручная-ux-проверка-mvp-потока.md`
- Изменения:
  - Первый поток `start -> topics -> first topic -> cards -> quiz -> result -> saved progress -> review` доведен до состояния ручной проверки.
  - Добавлена ясность UX: метка первой темы, объяснение заблокированного квиза, условие завершения темы и понятный статус `needs_native_review`.
  - Повторение и сброс прогресса включены в ручной сценарий приемки.
- Проверки:
  - `npm run lint` прошел.
  - `npm run build` прошел вне sandbox.
